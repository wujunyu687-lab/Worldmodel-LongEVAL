interface:
  display_name: "WorldArena Eval"
  short_description: "Auto-install and run WorldArena evaluation"
  default_prompt: "Use $worldarena-eval to ask for missing inputs, install dependencies and checkpoints, then run WorldArena evaluation end to end."
